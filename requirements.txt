# requirements.txt
#
# PURPOSE: ML model training only (scripts/train_model.py).
# This file is NOT used by the Vercel deployment.
#
# The live judge deploy (iie-web-yono.vercel.app) is a pure Next.js app.
# ML inference runs in TypeScript edge routes using exported weights from
# src/data/model_weights.json — no Python runtime required at inference time.
#
# To retrain the model locally:
#   pip install -r requirements.txt
#   python scripts/train_model.py
#
scikit-learn==1.5.0
numpy==1.26.4
pandas==2.2.2
